Location: Work/DSW_Github_automation 
uvicorn main:app --reload --host 0.0.0.0 --port 8000

In a different terminal: ngrok http 8000

Copy the long value on the left, and add /webhook at the end. Add that to DSW under the URL of the services
